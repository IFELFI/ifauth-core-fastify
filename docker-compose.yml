version: '3'

services:
  ifauth-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ifauth-api
    container_name: ifauth_api
    volumes:
      - ./logs:/app/logs
    ports:
      - '3000:3000'
    env_file:
      - ./.env
    restart: always
    networks:
      - ifelfi-network

networks:
  ifelfi-network:
    external: true
